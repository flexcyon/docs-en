---
title: 불투명도
---

에디터의 불투명도 관련 style settings를 모두 모아 둔 곳입니다.

허용되는 형식: x.y, rem

## 내비게이션

```md
Style Settings
|-- flexcyon://Editor
|   |-- Opacity
```

## 환경 설정 옵션

### 흐리게 표시된 요소의 불투명도

대상 CSS 변수: `var(--dimmed)`

기본값: 0.55

<span style="opacity: 0.55">샘플 흐리게 표시된 요소</span>

### 블록 레이블의 불투명도
대상 CSS 클래스: `var(--flexcyon-block-label-opacity)`

기본값: 0.55

<span style="opacity: 0.55">샘플 흐리게 표시된 요소</span>

### 새 탭에서 상태바 불투명도

대상 CSS 변수: `var(--flexcyon-status-new-tab-opacity)`

기본값: 0.55

### 콜아웃 메타데이터 배경 불투명도

대상 CSS 변수: `var(--flexcyon-callout-bg-opacity)`

기본값: 20%

<span style="opacity: 20%">샘플 배경 불투명도</span>

### 활성 줄 강조 불투명도

활성 줄 강조 배경의 불투명도입니다.

대상 CSS 변수: `var(--flexcyon-highlight-active-line-opacity)`

기본값: 7.5%

### 애니메이션 초기 불투명도

대상 CSS 변수: `var(--flexcyon-anim-start-opacity)`

기본값: 0.55

### 대체 링크 스타일 불투명도

대상 CSS 변수: `var(--flexcyon-alt-link-opacity)`

기본값: 55%

### 커뮤니티 아이템(비선택 상태)의 불투명도

대상 CSS 변수: `var(--flexcyon-comm-item-opacity)`

기본값: 0.89

<span style="opacity: 0.89">커뮤니티 아이템(비선택 상태)의 샘플 불투명도</span>
